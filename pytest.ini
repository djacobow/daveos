[pytest]
testpaths = tests
python_files = test_*.py
addopts = --strict-markers
cache_dir = build/pytest-cache
markers =
    hil: explicitly selected hardware-in-the-loop tests
    slow: longer stress or firmware update tests
