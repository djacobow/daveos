# UART and USB consoles with watchdog health: the STM32 default.
# Layer after the board cross file:
#   meson setup build/x --cross-file meson/stm32.ini --cross-file meson/profiles/console.ini
[project options]
features = ['uart', 'usb', 'health']
