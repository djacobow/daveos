# A/B firmware for the automated hardware tests: every console, networking and
# TCP firmware updates. tests/hil requires these features.
[project options]
bootloader = true
features = ['uart', 'usb', 'health', 'net', 'tcp', 'ota']
